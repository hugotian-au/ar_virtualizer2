using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using MLAPI;
using MLAPI.Messaging;
using MLAPI.NetworkVariable;
using MLAPI.Prototyping;
using Microsoft.MixedReality.Toolkit.UI;
using Microsoft.MixedReality.Toolkit.Input;

public class DynamicObjectPlayer : NetworkBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // var script = gameObject.AddComponent<ObjectManipulator>();
        // var script2 = gameObject.AddComponent<NearInteractionGrabbable>();
        // var script3 = gameObject.AddComponent<NetworkTransform>();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
